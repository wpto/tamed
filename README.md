# tamed - imageboard engine

## implemented

```
restapi /api
  GET /posts
  GET /posts/:id
  POST /posts
  PATCH /posts/:id
  DELETE /posts/:id

load media /media/:id
```

## todo

```
+ sql store
+ web client
+ upload vid
+ preview output for img/vid
```